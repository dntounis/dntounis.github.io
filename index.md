---
layout: home
title: Dimitris Ntounis
description: PhD Candidate in Physics at Stanford University, researching Higgs boson physics and future particle colliders.
---

<!-- Affiliation Logos -->
<div class="affiliation-logos">
    <a href="https://www.stanford.edu/" target="_blank" title="Stanford University">
        <img src="/assets/images/logos/SUSig.png" alt="Stanford University" class="affiliation-logo">
    </a>
    <a href="https://www6.slac.stanford.edu/" target="_blank" title="SLAC National Accelerator Laboratory">
        <img src="/assets/images/logos/SLAC_short_red.png" alt="SLAC National Accelerator Laboratory" class="affiliation-logo">
    </a>
    <a href="https://home.cern/" target="_blank" title="CERN">
        <img src="/assets/images/logos/CERNBlue.png" alt="CERN" class="affiliation-logo">
    </a>
    <a href="https://atlas.cern/" target="_blank" title="ATLAS Experiment">
        <img src="/assets/images/logos/ATLAS.png" alt="ATLAS Experiment" class="affiliation-logo">
    </a>
    <a href="https://web.slac.stanford.edu/c3/" target="_blank" title="Cool Copper Collider (C³)">
        <img src="/assets/images/logos/ccc.png" alt="Cool Copper Collider" class="affiliation-logo">
    </a>
</div>

<h1 class="cardinal-red-title">About</h1>

My name is Dimitrios (but most people call me Dimitris) Ntounis [*/di-MEE-tris DOO-nis/*] and I come from Athens, Greece. I am currently a PhD Candidate in the <a href="https://physics.stanford.edu/" target="_blank">Physics Department</a> at <a href="https://www.stanford.edu/" target="_blank">Stanford University</a>. I am a researcher at <a href="https://www6.slac.stanford.edu/" target="_blank">SLAC National Accelerator Laboratory</a> working with the <a href="https://atlas.slac.stanford.edu/" target="_blank">SLAC ATLAS</a> group. I'm also pursuing a PhD Minor in Computer Science at Stanford.

You can find a few key points about me below or read my [Projects](/projects) page or <a href="{{ site.cv_pdf }}" target="_blank">CV</a> for more details.

## Education
- PhD in Physics, Stanford University, *in progress*
- PhD Minor in Computer Science, Stanford University, *in progress*
- MSc in Physics, Stanford University, 2024
- BSc in Physics, National and Kapodistrian University of Athens, 2021

## Research Interests
- Higgs Boson Physics with the ATLAS experiment at the CERN LHC
- Future electron-positron colliders
- Machine Learning for Science

<h1 class="cardinal-red-title">Research</h1>

My research lies at the intersection of experimental particle physics, accelerator science, and machine learning. I am driven by a fundamental question: *What are the building blocks of the Universe, and how do they interact?*

**Higgs Boson Physics at the LHC**: I analyze petabyte-scale datasets from the <a href="https://atlas.cern/" target="_blank">ATLAS</a> detector at CERN's Large Hadron Collider (LHC) to probe the properties of the <a href="https://en.wikipedia.org/wiki/Higgs_boson" target="_blank">Higgs boson</a>—the particle intimately tied to the mechanism that gives fundamental particles their mass. My work focuses on precision measurements of Higgs production in association with vector bosons, using advanced statistical techniques to extract signals from challenging backgrounds.

**Next-Generation Colliders**: Looking beyond the LHC, I contribute to the development of future electron-positron colliders designed to achieve unprecedented precision in Higgs, electroweak, and top quark measurements. My contributions include comprehensive studies of the beam dynamics, luminosity optimization, and beam-induced background characterization for the <a href="https://web.slac.stanford.edu/c3/" target="_blank">Cool Copper Collider (C³)</a>, a newly proposed US-led linear collider concept. I also benchmark jet flavor tagging algorithms across different detector configurations to inform optimal detector design.

**AI/ML for Discovery**: I leverage state-of-the-art machine learning techniques to extract maximum physics insight from available data. This includes developing novel algorithms for particle identification that enhance our sensitivity to new physics signatures.

<h1 class="cardinal-red-title">In the News</h1>

<div class="news-coverage">
    <div class="news-item-featured">
        <a href="https://atlas.cern/Updates/Briefing/Jetting-Higgs-Boson" target="_blank" class="news-link-card">
            <div class="news-source">ATLAS Experiment</div>
            <div class="news-headline">Jetting towards the Higgs boson</div>
            <div class="news-description">ATLAS explores high-energy Higgs boson production in the VH channel using advanced jet reconstruction techniques.</div>
        </a>
    </div>
    <div class="news-item-featured">
        <a href="https://www6.slac.stanford.edu/news/2023-11-03-physicists-ask-can-we-make-particle-collider-more-energy-efficient" target="_blank" class="news-link-card">
            <div class="news-source">SLAC National Accelerator Laboratory</div>
            <div class="news-headline">Can we make a particle collider more energy efficient?</div>
            <div class="news-description">Researchers explore sustainability strategies for future particle colliders, including the Cool Copper Collider concept.</div>
        </a>
    </div>
    <div class="news-item-featured">
        <a href="https://www.asme.org/topics-resources/content/research-continues-while-reducing-particle-collider-carbon-footprints" target="_blank" class="news-link-card">
            <div class="news-source">ASME</div>
            <div class="news-headline">Research Continues While Reducing Particle Collider Carbon Footprints</div>
            <div class="news-description">How physicists are working to make next-generation colliders more environmentally sustainable.</div>
        </a>
    </div>
</div>

<h1 class="cardinal-red-title">Research Network</h1>

<p>Interactive visualization of my research collaborations from <a href="https://inspirehep.net/authors/2649182" target="_blank">INSPIRE-HEP</a>. Excludes large collaboration papers (ATLAS/CMS) to show individual co-authors.</p>

<div id="collaboration-network" class="collaboration-viz">
    <div class="viz-controls">
        <label class="viz-toggle">
            <input type="checkbox" id="include-collab-toggle">
            <span class="toggle-label">Include ATLAS/CMS collaboration papers</span>
        </label>
    </div>
    <div class="viz-stats" id="viz-stats">
        <span class="stat-item"><strong id="paper-count">-</strong> papers</span>
        <span class="stat-item"><strong id="collaborator-count">-</strong> collaborators</span>
        <span class="stat-item"><strong id="citation-count">-</strong> citations</span>
    </div>
    <div id="viz-container">
        <div class="viz-loading">
            <i class="fas fa-spinner fa-spin"></i> Loading from INSPIRE-HEP...
        </div>
    </div>
    <div class="viz-legend">
        <span class="legend-item"><span class="legend-dot" style="background: #8C1515;"></span> You</span>
        <span class="legend-item"><span class="legend-dot" style="background: #2E5984;"></span> Collaborators</span>
        <span class="legend-item"><span class="legend-dot" style="background: #F9A825;"></span> Frequent (3+ papers)</span>
        <span class="legend-item" id="collab-legend" style="display: none;"><span class="legend-dot" style="background: #4CAF50;"></span> Collaborations</span>
    </div>
    <p class="viz-note" id="viz-note">Showing individual co-authors only. Papers with &gt;20 authors (ATLAS/CMS) are excluded.</p>
</div>

<script>
(function() {
    const INSPIRE_BAI = 'Dimitris.Ntounis.1';
    const AUTHOR_NAME = 'D. Ntounis';
    const COLLAB_THRESHOLD = 20; // Papers with more authors than this are considered collaboration papers

    // Cache for INSPIRE data
    let rawPapers = null;
    let includeCollabPapers = false;

    async function fetchInspireData() {
        try {
            // Fetch publications from INSPIRE-HEP API using BAI (INSPIRE author identifier)
            const response = await fetch(
                `https://inspirehep.net/api/literature?sort=mostrecent&size=100&q=a%20${INSPIRE_BAI}`
            );
            const data = await response.json();

            if (!data.hits || !data.hits.hits) {
                throw new Error('No data returned from INSPIRE');
            }

            return data.hits.hits;
        } catch (error) {
            console.error('Error fetching INSPIRE data:', error);
            return null;
        }
    }

    function processInspireData(papers, includeCollab = false) {
        const collaboratorMap = new Map();
        const collaborationSet = new Set(); // Track large collaborations (ATLAS, CMS, etc.)
        const nodes = [];
        const links = [];
        let totalCitations = 0;
        let includedPapers = 0;
        let collabPapersCount = 0;

        // Process each paper
        papers.forEach(paper => {
            const metadata = paper.metadata;
            const authors = metadata.authors || [];
            const citations = metadata.citation_count || 0;
            const isCollabPaper = authors.length > COLLAB_THRESHOLD;

            if (isCollabPaper) {
                collabPapersCount++;
                if (!includeCollab) return; // Skip if not including collaboration papers

                // For collaboration papers, just track the collaboration name
                const collaborations = metadata.collaborations || [];
                collaborations.forEach(collab => {
                    const collabName = collab.value || collab;
                    if (collabName && !collaborationSet.has(collabName)) {
                        collaborationSet.add(collabName);
                    }
                });
                totalCitations += citations;
                includedPapers++;
                return;
            }

            // Non-collaboration paper
            totalCitations += citations;
            includedPapers++;

            // Extract co-authors (excluding self)
            authors.forEach(author => {
                const fullName = author.full_name;
                if (!fullName) return;

                // Skip if it's the main author
                if (fullName.toLowerCase().includes('ntounis')) return;

                // Create a normalized key for the author
                const key = fullName.toLowerCase().replace(/[^a-z]/g, '');

                if (collaboratorMap.has(key)) {
                    const collab = collaboratorMap.get(key);
                    collab.papers++;
                    collab.citations += citations;
                } else {
                    collaboratorMap.set(key, {
                        id: key,
                        name: fullName,
                        papers: 1,
                        citations: citations,
                        group: 'collaborator'
                    });
                }
            });
        });

        // Add self node
        nodes.push({
            id: 'self',
            name: AUTHOR_NAME,
            group: 'self',
            papers: includedPapers,
            citations: totalCitations
        });

        // Convert collaborators to nodes and create links
        // Sort by paper count and take top collaborators for cleaner visualization
        const maxCollaborators = includeCollab ? 30 : 25;
        const sortedCollaborators = Array.from(collaboratorMap.values())
            .sort((a, b) => b.papers - a.papers)
            .slice(0, maxCollaborators);

        sortedCollaborators.forEach(collab => {
            // Mark frequent collaborators
            if (collab.papers >= 3) {
                collab.group = 'frequent';
            }
            nodes.push(collab);
            links.push({
                source: 'self',
                target: collab.id,
                value: collab.papers
            });
        });

        // Add collaboration nodes if including collab papers
        if (includeCollab && collaborationSet.size > 0) {
            collaborationSet.forEach(collabName => {
                const collabId = collabName.toLowerCase().replace(/[^a-z]/g, '');
                nodes.push({
                    id: collabId,
                    name: collabName,
                    group: 'collaboration',
                    papers: collabPapersCount
                });
                links.push({
                    source: 'self',
                    target: collabId,
                    value: collabPapersCount
                });
            });
        }

        // Update stats display
        const noteEl = document.getElementById('viz-note');
        const collabLegend = document.getElementById('collab-legend');

        if (includeCollab) {
            document.getElementById('paper-count').textContent = papers.length;
            noteEl.textContent = `Showing all papers including ${collabPapersCount} collaboration papers (>${COLLAB_THRESHOLD} authors).`;
            collabLegend.style.display = 'inline-flex';
        } else {
            document.getElementById('paper-count').textContent = includedPapers;
            noteEl.textContent = `Showing individual co-authors only. ${collabPapersCount} papers with >${COLLAB_THRESHOLD} authors (ATLAS/CMS) are excluded.`;
            collabLegend.style.display = 'none';
        }
        document.getElementById('collaborator-count').textContent = sortedCollaborators.length + (includeCollab ? collaborationSet.size : 0);
        document.getElementById('citation-count').textContent = totalCitations.toLocaleString();

        return { nodes, links };
    }

    function createNetwork(data) {
        const container = document.getElementById('viz-container');
        if (!container) return;
        container.innerHTML = '';

        if (!data || data.nodes.length === 0) {
            container.innerHTML = '<p class="viz-error">Could not load collaboration data. <a href="https://inspirehep.net/authors/2649182" target="_blank">View on INSPIRE-HEP</a></p>';
            return;
        }

        const width = container.clientWidth || 600;
        const height = 500;
        const centerX = width / 2;
        const centerY = height / 2;

        const svg = d3.select('#viz-container')
            .append('svg')
            .attr('width', width)
            .attr('height', height)
            .attr('viewBox', `0 0 ${width} ${height}`);

        // Add zoom behavior
        const g = svg.append('g');
        svg.call(d3.zoom()
            .scaleExtent([0.5, 3])
            .on('zoom', (event) => g.attr('transform', event.transform)));

        // Stronger forces for better spacing
        const simulation = d3.forceSimulation(data.nodes)
            .force('link', d3.forceLink(data.links).id(d => d.id).distance(d => 120 + (5 - Math.min(d.value, 5)) * 15))
            .force('charge', d3.forceManyBody().strength(-300))
            .force('center', d3.forceCenter(centerX, centerY))
            .force('collision', d3.forceCollide().radius(d => getNodeRadius(d) + 35))
            .force('x', d3.forceX(centerX).strength(0.05))
            .force('y', d3.forceY(centerY).strength(0.05));

        function getNodeRadius(d) {
            if (d.group === 'self') return 28;
            if (d.group === 'collaboration') return 20;
            if (d.group === 'frequent') return 10 + Math.min(d.papers, 5) * 1.5;
            return 7 + Math.min(d.papers, 5) * 1.5;
        }

        function getNodeColor(d) {
            if (d.group === 'self') return '#8C1515';
            if (d.group === 'collaboration') return '#4CAF50';
            if (d.group === 'frequent') return '#F9A825';
            return '#2E5984';
        }

        function formatName(d) {
            if (d.group === 'self') return 'You';
            if (d.group === 'collaboration') return d.name;
            // Format: "F. Lastname"
            const parts = d.name.split(/[,\s]+/).filter(p => p.length > 0);
            if (parts.length >= 2) {
                // Handle "Lastname, Firstname" format
                if (d.name.includes(',')) {
                    return parts[1].charAt(0) + '. ' + parts[0];
                }
                return parts[0].charAt(0) + '. ' + parts[parts.length - 1];
            }
            return d.name;
        }

        // Draw links first (behind nodes)
        const link = g.append('g')
            .attr('class', 'links')
            .selectAll('line')
            .data(data.links)
            .join('line')
            .attr('stroke', '#999')
            .attr('stroke-opacity', 0.3)
            .attr('stroke-width', d => Math.sqrt(Math.min(d.value, 8)) * 1.2);

        // Create node groups
        const node = g.append('g')
            .attr('class', 'nodes')
            .selectAll('g')
            .data(data.nodes)
            .join('g')
            .attr('class', 'node-group')
            .style('cursor', 'pointer')
            .call(d3.drag()
                .on('start', dragstarted)
                .on('drag', dragged)
                .on('end', dragended));

        // Add circles
        node.append('circle')
            .attr('r', d => getNodeRadius(d))
            .attr('fill', d => getNodeColor(d))
            .attr('stroke', '#fff')
            .attr('stroke-width', 2)
            .attr('opacity', 0.9);

        // Add label backgrounds (white rectangles behind text)
        const labelGroups = node.append('g')
            .attr('class', 'label-group');

        // Background rect (will be sized after text is rendered)
        labelGroups.append('rect')
            .attr('class', 'label-bg')
            .attr('fill', 'rgba(255,255,255,0.9)')
            .attr('rx', 3)
            .attr('ry', 3)
            .attr('opacity', 0);

        // Add text labels
        labelGroups.append('text')
            .attr('class', 'node-label')
            .text(d => formatName(d))
            .attr('text-anchor', 'middle')
            .attr('dominant-baseline', 'middle')
            .attr('font-size', d => {
                if (d.group === 'self') return '11px';
                if (d.group === 'collaboration') return '10px';
                if (d.group === 'frequent') return '9px';
                return '8px';
            })
            .attr('font-family', 'Inter, -apple-system, sans-serif')
            .attr('font-weight', d => (d.group === 'self' || d.group === 'collaboration') ? '600' : '500')
            .attr('fill', '#333')
            .attr('pointer-events', 'none');

        // Size and position the background rectangles
        labelGroups.each(function(d) {
            const group = d3.select(this);
            const text = group.select('text');
            const bbox = text.node().getBBox();
            const padding = 3;

            group.select('rect')
                .attr('x', bbox.x - padding)
                .attr('y', bbox.y - padding)
                .attr('width', bbox.width + padding * 2)
                .attr('height', bbox.height + padding * 2)
                .attr('opacity', d.group === 'self' ? 0 : 0.85);
        });

        // Tooltip with more info
        node.append('title')
            .text(d => {
                if (d.group === 'collaboration') {
                    return `${d.name} Collaboration\n${d.papers} paper${d.papers > 1 ? 's' : ''}`;
                }
                if (d.group === 'self') {
                    return `${d.name}\n${d.papers} papers\n${d.citations.toLocaleString()} total citations`;
                }
                return `${d.name}\n${d.papers} paper${d.papers > 1 ? 's' : ''} together${d.citations ? `\n${d.citations} citations` : ''}`;
            });

        // Hover effects
        node.on('mouseenter', function(event, d) {
            // Highlight this node
            d3.select(this).select('circle')
                .attr('stroke', getNodeColor(d))
                .attr('stroke-width', 3);

            // Highlight connected links
            link.attr('stroke-opacity', l =>
                (l.source.id === d.id || l.target.id === d.id) ? 0.8 : 0.1
            ).attr('stroke', l =>
                (l.source.id === d.id || l.target.id === d.id) ? getNodeColor(d) : '#999'
            );

            // Dim other nodes
            node.select('circle').attr('opacity', n =>
                n.id === d.id || data.links.some(l =>
                    (l.source.id === d.id && l.target.id === n.id) ||
                    (l.target.id === d.id && l.source.id === n.id)
                ) ? 1 : 0.3
            );
        })
        .on('mouseleave', function() {
            // Reset all
            node.select('circle')
                .attr('stroke', '#fff')
                .attr('stroke-width', 2)
                .attr('opacity', 0.9);
            link.attr('stroke-opacity', 0.3)
                .attr('stroke', '#999');
        });

        // Update positions on tick
        simulation.on('tick', () => {
            link
                .attr('x1', d => d.source.x)
                .attr('y1', d => d.source.y)
                .attr('x2', d => d.target.x)
                .attr('y2', d => d.target.y);

            node.attr('transform', d => `translate(${d.x},${d.y})`);

            // Position labels to avoid center (radial outward)
            labelGroups.attr('transform', function(d) {
                if (d.group === 'self') {
                    return `translate(0, ${getNodeRadius(d) + 14})`;
                }
                // Calculate angle from center
                const dx = d.x - centerX;
                const dy = d.y - centerY;
                const angle = Math.atan2(dy, dx);
                const labelDist = getNodeRadius(d) + 12;
                const lx = Math.cos(angle) * labelDist;
                const ly = Math.sin(angle) * labelDist;
                return `translate(${lx}, ${ly})`;
            });
        });

        function dragstarted(event, d) {
            if (!event.active) simulation.alphaTarget(0.3).restart();
            d.fx = d.x;
            d.fy = d.y;
        }

        function dragged(event, d) {
            d.fx = event.x;
            d.fy = event.y;
        }

        function dragended(event, d) {
            if (!event.active) simulation.alphaTarget(0);
            d.fx = null;
            d.fy = null;
        }
    }

    function updateVisualization() {
        if (!rawPapers) return;
        const data = processInspireData(rawPapers, includeCollabPapers);
        createNetwork(data);
    }

    async function initViz() {
        if (typeof d3 === 'undefined') {
            setTimeout(initViz, 100);
            return;
        }

        // Fetch raw data
        rawPapers = await fetchInspireData();
        if (!rawPapers) {
            document.getElementById('viz-container').innerHTML = '<p class="viz-error">Could not load data from INSPIRE-HEP. <a href="https://inspirehep.net/authors/2649182" target="_blank">View profile directly</a></p>';
            return;
        }

        // Initial render
        updateVisualization();

        // Toggle event listener
        const toggle = document.getElementById('include-collab-toggle');
        if (toggle) {
            toggle.addEventListener('change', function() {
                includeCollabPapers = this.checked;
                updateVisualization();
            });
        }

        // Recreate on resize
        let resizeTimeout;
        window.addEventListener('resize', () => {
            clearTimeout(resizeTimeout);
            resizeTimeout = setTimeout(updateVisualization, 250);
        });
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initViz);
    } else {
        initViz();
    }
})();
</script>

<h1 class="cardinal-red-title">From the Blog</h1>

<div id="substack-posts" class="blog-posts-grid">
    <p class="loading-text">Loading recent posts...</p>
</div>

<div class="blog-cta">
    <a href="https://dimitrisntounis.substack.com" target="_blank" class="blog-subscribe-btn">
        <i class="fas fa-envelope"></i> Subscribe on Substack
    </a>
</div>

<script>
(function() {
    async function fetchSubstackPosts() {
        const container = document.getElementById('substack-posts');

        try {
            // Using RSS2JSON service to parse Substack RSS
            const response = await fetch('https://api.rss2json.com/v1/api.json?rss_url=https://dimitrisntounis.substack.com/feed');
            const data = await response.json();

            if (data.status === 'ok' && data.items && data.items.length > 0) {
                const posts = data.items.slice(0, 3); // Get latest 3 posts

                container.innerHTML = posts.map(post => {
                    const date = new Date(post.pubDate).toLocaleDateString('en-US', {
                        year: 'numeric',
                        month: 'short',
                        day: 'numeric'
                    });
                    const excerpt = post.description
                        ? post.description.replace(/<[^>]*>/g, '').substring(0, 120) + '...'
                        : '';

                    return `
                        <article class="blog-post-card">
                            <a href="${post.link}" target="_blank" class="blog-post-link">
                                <div class="blog-post-date">${date}</div>
                                <h3 class="blog-post-title">${post.title}</h3>
                                <p class="blog-post-excerpt">${excerpt}</p>
                                <span class="blog-read-more">Read more →</span>
                            </a>
                        </article>
                    `;
                }).join('');
            } else {
                container.innerHTML = `
                    <div class="blog-placeholder">
                        <p>Visit my <a href="https://dimitrisntounis.substack.com" target="_blank">Substack</a> for the latest posts on physics, research, and more.</p>
                    </div>
                `;
            }
        } catch (error) {
            console.log('Could not fetch Substack posts:', error);
            container.innerHTML = `
                <div class="blog-placeholder">
                    <p>Visit my <a href="https://dimitrisntounis.substack.com" target="_blank">Substack</a> for the latest posts on physics, research, and more.</p>
                </div>
            `;
        }
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', fetchSubstackPosts);
    } else {
        fetchSubstackPosts();
    }
})();
</script>

<div class="section-spacer"></div>

<div class="contact-cta">
    <p>Interested in collaborating or have questions? <a href="/contact/">Get in touch</a> or <a href="/contact/#schedule-meeting">schedule a meeting</a>.</p>
</div>
